"""Слой доступа к данным (repository) приложения ``vehicles``.

Репозитории инкапсулируют построение SQL-запросов и работу с ORM. Они НЕ знают
про HTTP: методы поиска возвращают ORM-объект или ``None`` (а не бросают
``HTTPException``). Преобразование ``None`` → ``404`` — ответственность слоя
эндпоинтов.

Разделение ответственностей (SRP):

- ``repositories/`` — доступ к данным (запросы, ``selectinload``, upsert);
- ``services/``     — бизнес-логика (транзакции, инварианты, оркестрация);
- ``api/<x>/mappers.py`` — преобразование ORM → Pydantic-схема;
- ``api/<x>/endpoints.py`` — только HTTP (роутинг, статусы, ``HTTPException``).
"""
