"""Слой бизнес-логики (services) приложения ``accounts``.

Сервисы владеют транзакциями и инвариантами (аутентификация, регистрация,
обновление профиля). Они не знают про HTTP: возвращают ORM-объект / ``None`` /
доменные значения, а HTTP-исключения поднимает только слой эндпоинтов.
"""
